import { render } from "@testing-library/react";
import { GifExpertApp } from "../src/GifExpertApp";

describe("Pruebas en <GifExpertApp />", () => {
  test("Prueba pendiente", () => {
    render(<GifExpertApp />);
    screen.debug();
    // expect(1).toBe(1);
  });
});
